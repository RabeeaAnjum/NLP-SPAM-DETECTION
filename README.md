# NLP-SPAM-DETECTION
This project implements a complete (NLP) pipeline to classify SMS messages as Spam or Ham (Legitimate) using Machine Learning. The system applies text preprocessing techniques, converts text into numerical features using TF-IDF Vectorization, and trains a Multinomial Naive Bayes classifier to detect spam messages with high accuracy.
